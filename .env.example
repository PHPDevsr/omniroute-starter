# Host port exposed only to localhost.
OMNIROUTE_PORT=30128

# Public URL served by Nginx.
NEXT_PUBLIC_BASE_URL=https://omniroute.example.com

# Generate with: openssl rand -hex 32
OMNIROUTE_WS_BRIDGE_SECRET=change-me

# OmniRoute memory setting.
OMNIROUTE_MEMORY_MB=4096
